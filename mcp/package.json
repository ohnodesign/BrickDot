{
  "name": "brickdot-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server that lets Claude read and update BrickDot through the app's loopback bridge.",
  "type": "module",
  "bin": { "brickdot-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
